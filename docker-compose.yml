# 本地开发依赖：一键启动 MongoDB（后续上 BullMQ 时再启用 Redis）
services:
  mongo:
    image: mongo:7
    ports:
      - '27017:27017'
    volumes:
      - mongo_data:/data/db

  # redis:
  #   image: redis:7
  #   ports:
  #     - '6379:6379'

volumes:
  mongo_data:
