{
  "name": "ad-optimizer",
  "version": "0.1.0",
  "description": "多平台（Meta/Google/TikTok）广告数据对比与归因工具：官方只读API取数 → 统一模型 → 波动分析 → 对比结论与建议",
  "scripts": {
    "build": "tsc",
    "demo": "ts-node src/demo.ts"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "axios": "^1.7.0",
    "mongoose": "^8.4.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
